# OS / editor noise
.DS_Store

# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Local virtual environments
.venv/

# Upstream checkout is managed as an optional local submodule/worktree
third_party/

# Local model weights / exported ONNX graphs. Keep .onnx external data next to
# the .onnx file, but keep the whole tree out of git
models/onnx/
models/hf/

# Generated outputs: benchmark WAVs/JSON, reports, smoke samples, and logs
artifacts/

# Generate-path JSONL traces are local comparison data
traces/

# Local package/build outputs
*.egg-info/
build/
dist/
